import React from 'react';
import PropTypes from 'prop-types';

const Votation = props => {
  const { title, date, description } = votation;

  return <div></div>;
};

Votation.propTypes = {};

export default Votation;
